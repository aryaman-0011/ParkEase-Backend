package com.parkease.payment_service.enums;

/**
 * Enum defining payment lifecycle states: PENDING, COMPLETED, FAILED, REFUNDED.
 */
public enum PaymentStatus {
    PENDING,
    SUCCESS,
    FAILED,
    REFUNDED
}
